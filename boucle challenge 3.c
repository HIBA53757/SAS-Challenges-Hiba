#include <stdio.h>
#include <stdlib.h>

int main()
{   int n,i,s;
       printf("enterer un nombre:");
       scanf("%d",&n);
    for(i=1;i<=n;i++){
        s=(n*(n+1))/2;}
        printf("Nombres Naturels de %d est:%d",n,s);

    return 0;
}
