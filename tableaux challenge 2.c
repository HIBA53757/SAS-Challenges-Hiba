#include <stdio.h>
#include <stdlib.h>

int main()
{   int i,taille;
    printf("entrer la taille:");
    scanf("%d",&taille);
    int T[taille];
for (i=0;i<taille;i++){
    printf("veuiller enter les element du tableau:");
    scanf("%d",&T[taille]);
}
    return 0;
}
